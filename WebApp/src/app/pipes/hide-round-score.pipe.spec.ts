import { HideRoundScorePipe } from './hide-round-score.pipe';

describe('HideRoundScorePipe', () => {
  it('create an instance', () => {
    const pipe = new HideRoundScorePipe();
    expect(pipe).toBeTruthy();
  });
});
